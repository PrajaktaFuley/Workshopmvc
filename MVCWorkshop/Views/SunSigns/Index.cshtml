@model IEnumerable<MVCWorkshop.Models.SunSign>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SignName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SignDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SignName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SignDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SignId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SignId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SignId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
